server:
  http_listen_port: 3200
  grpc_listen_port: 4317

distributor:
  receivers: [otlp]

receivers:
  otlp:
    protocols:
      grpc:
      http:

storage:
  trace:
    backend: filesystem
    path: /var/tempo/traces
